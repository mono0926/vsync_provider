name: example
description: Example
version: 1.0.0+1
environment:
  sdk: ">=2.2.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  vsync_provider:
    path: ../../vsync_provider
  disposable_provider:
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
